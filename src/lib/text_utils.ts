export async function splitTextIntoSemantics(text: string): Promise<string[]> {
    // Implémentez votre logique pour découper le texte en morceaux sémantiques
    // Exemple simplifié :
    const chunks = text.split(/\s+/); // Divise le texte en mots (adapté à vos besoins)
    return chunks;
  }
  